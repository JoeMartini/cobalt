version: '3'
services:
  cobalt:
    build:
      context: .
      dockerfile: Dockerfile
    image: cobalt:latest
    container_name: cobalt
    restart: always
    ports:
      - "9000:9000"
    volumes:
      - ./:/app
    environment:
      - NODE_ENV=production
